{
  "name": "accounting-automation-system",
  "version": "1.0.0",
  "description": "An accounting automation system with React, Express, and MongoDB",
  "scripts": {
    "install-all": "npm install --prefix server && npm install --prefix client",
    "server": "npm start --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "npx concurrently \"npm run dev --prefix client\" \"npm start --prefix server\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
